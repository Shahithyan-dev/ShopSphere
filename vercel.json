{
  "version": 2,
  "buildCommand": "npm install --prefix shopsphere-react && npm run build --prefix shopsphere-react",
  "outputDirectory": "shopsphere-react/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
