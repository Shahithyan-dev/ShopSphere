{
  "version": 2,
  "buildCommand": "npm install --prefix shopsphere && npm install --prefix shopsphere-react && npm run build --prefix shopsphere-react",
  "outputDirectory": "shopsphere-react/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
